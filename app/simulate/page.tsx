"use client";

import V32StubPage from "@/components/layout/V32StubPage";

export default function SimulatePage() {
  return (
    <V32StubPage
      pageTitle="Simulate"
      description="A deterministic what-if scenario engine — real computed downstream effects, never an AI guess — hasn't been built yet. Today, the closest real equivalent is the Cash Forecast page."
    />
  );
}
