"use client";

import V32StubPage from "@/components/layout/V32StubPage";

export default function DiscoverPage() {
  return (
    <V32StubPage
      pageTitle="Discover"
      description="A proactive feed of patterns Starlane finds in your data on its own — opportunities, leakage, risks — hasn't been built yet."
    />
  );
}
