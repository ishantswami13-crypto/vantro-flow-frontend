"use client";

import V32StubPage from "@/components/layout/V32StubPage";

export default function MissionsPage() {
  return (
    <V32StubPage
      pageTitle="Missions"
      description="Outcome-based workspaces that track a goal's real progress across the underlying data hasn't been built yet."
    />
  );
}
