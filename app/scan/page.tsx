"use client";

import V32StubPage from "@/components/layout/V32StubPage";

export default function ScanPage() {
  return (
    <V32StubPage
      pageTitle="Scan"
      description="Ask Starlane a question and get a real, evidence-backed answer over your connected data — this query composer hasn't been built yet. Today, the closest real equivalent is the Intelligence feed."
    />
  );
}
