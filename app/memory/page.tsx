"use client";

import V32StubPage from "@/components/layout/V32StubPage";

export default function MemoryPage() {
  return (
    <V32StubPage
      pageTitle="Memory"
      description="An entity-scoped event timeline sourced from your real audit trail hasn't been built yet."
    />
  );
}
