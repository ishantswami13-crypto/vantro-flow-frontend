"use client";

import V32StubPage from "@/components/layout/V32StubPage";

export default function BridgePage() {
  return (
    <V32StubPage
      pageTitle="The Bridge"
      description="The Bridge — a single home view of what changed, what needs a decision, and what's already prepared for you — hasn't been built yet. Today, the closest real equivalent is the Dashboard and Intelligence feed."
    />
  );
}
